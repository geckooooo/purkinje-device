package purkinje

// Turns on an LED for onDuration milliseconds,
// then off for offDuration milliseconds.
// Replacing this stub with actual hardware control logic.
func Blink(onDuration int, offDuration int) string {
	return "BLINK!"
}
